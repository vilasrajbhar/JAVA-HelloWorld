---
- hosts: vilasraj@175.25.2.5
  become: true
  become_user: root
  tasks:
    - name: copy all files from folder to remote host
      copy:
        src: /var/lib/jenkins/workspace/Docker-Ansible-Project/webapp/target/webapp.war
        dest: /opt/docker/webapp.war
